#include <iostream>
#include <cmath> // This is the math library
using namespace std;

int main()
{
    // cout << M_PI; // To find the value of PI
    // int a = 5, b = 7, c = 1;
    // cout << "The Min value is : " << min(a, b) << endl;
    // cout << "The Max value is : " << max(a, b) << endl;
    // cout << min(min(a, b), c) << endl; // To find the minimum of three numbers we use  nested function

    // int a = 25;
    // cout << "The Square root of the number is : " << sqrt(a) << endl;
    // int b = 343;
    // cout << "The Cube root of the number is : " << cbrt(b) << endl;

    // To Find the absolute value of the number
    // int a = -5;
    // cout << abs(a);

    // cout << pow(2, 3); // Power of the Number
    // float a = 16, b = 0.5;
    // cout << pow(a, b);

    float a = 7.99, b = -1.011;
    // cout << floor(a) << "  " << floor(b); ==> Floor function : Go down
    cout << ceil(a) << " " << ceil(b); //==> Ceil function : Go up

    return 0;
}